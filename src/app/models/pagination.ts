import { map } from "rxjs/operator/map";

export class Pagination {
    limits: [1, 2, 3, 4, 5];
    pages: [1, 2, 3, 4, 5];
    currentLimit: number;
    currentPage: number;

    constructor(currentLimit: number, currentPage: number) {
        this.limits = () => map(val => );
        this.pages = page;
        this.currentLimit = currentLimit;
        this.currentPage = currentPage;
    }

    function readArray(currentVal: number) {
        for (let _i = 0; _i < currentVal; _i++) {
            numbers[i] = i;
            return numbers;
        }
    }
}
